#include <stdio.h>
/**
*main - Prints a hexadecimal string
*Return: Always (Success)
*/
{
int c;
int i;
for (c = '10'; c <= '19'; c++)
for (i = '10'; i <= '19'; i++)
{
if ((i % 10) > (c % 10))
	putchar(c);
{
	putchar(c % 10) + '0');
	putchar(i % 10) + '0');
if (c != 18 || j != 19)
{
	putchar(',');
	putchar(',');
}
	putchar('\n');
return (0);
}
