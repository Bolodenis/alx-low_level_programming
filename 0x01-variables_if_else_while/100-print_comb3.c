#include <stdio.h>
/**
*main - Prints the numbers since 0 to 9
*Return: Always (Success)
*/
if ((c != '9') || (c == '9' && i != '9'))
{
	putchar(',');
	putchar(' ');
}
}
}
putchar('\n');
return (0);
}
